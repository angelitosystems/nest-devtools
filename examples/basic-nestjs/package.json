{
  "name": "basic-nestjs-example",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal NestJS app wired to NestJS DevTools.",
  "scripts": {
    "dev": "bun run src/main.ts",
    "start": "bun run src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@angelitosystems/nest-devtools": "workspace:*",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
