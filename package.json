{
  "name": "nest-devtools-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Real-time debugging, observability and developer tooling for NestJS applications.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "bun run --cwd packages/protocol build && bun run --cwd packages/core build && bun run --cwd packages/nestjs build && bun run --cwd packages/cli build && bun run --cwd examples/basic-nestjs build",
    "test": "bun test packages examples",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "e2e": "bun scripts/e2e-smoke.mjs",
    "dev:dashboard": "bun run --cwd apps/dashboard dev",
    "dev:server": "bun run --cwd packages/cli dev",
    "dev:example": "bun run --cwd examples/basic-nestjs dev",
    "docs:dev": "bun run --cwd apps/docs dev",
    "docs:build": "bun run --cwd apps/docs build",
    "typecheck:docs": "tsc -p apps/docs/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.9.3"
  }
}
